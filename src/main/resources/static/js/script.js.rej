diff a/src/main/resources/static/js/script.js b/src/main/resources/static/js/script.js	(rejected hunks)
@@ -405,13 +405,13 @@
 			var checkResult = $("#mail_check_input_box_warn"); // 비교 결과 
 			$.ajax({
 				type: "GET",
-				url: "../mail_du_Check?email=" + email,
+				url: "../mail_du_Check?email=" + email + "&user=" + user,
 				success: function(data) {
 					console.log("data : " + data);
 					mail_duCheck = data;
-
+					
 					if (mail_duCheck == "1") {
-						checkResult.html("가입 가능한 이메일 입니다.");
+						checkResult.html("해당 이메일로 인증번호를 발송하였습니다.");
 						checkResult.attr("class", "correct");
 						mail_duCheck = true;
 						$.ajax({
